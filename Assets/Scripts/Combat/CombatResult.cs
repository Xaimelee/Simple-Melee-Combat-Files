using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace A.Combat
{
    //: The attacker was successful or not successful
    public enum CombatResult { Hit, Blocked }
    public enum CombatRollResult { Succeded, Failed }
}
